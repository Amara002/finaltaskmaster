<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.taskmaster.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/allTaskActivity"
        android:name="com.example.taskmaster.AllTaskActivity"
        android:label="activity_all_task"
        tools:layout="@layout/activity_all_task" />
    <activity
        android:id="@+id/addTaskActivity"
        android:name="com.example.taskmaster.AddTaskActivity"
        android:label="activity_add_task"
        tools:layout="@layout/activity_add_task" />
</navigation>